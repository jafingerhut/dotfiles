-- Lua comment

-- constants: false, true
-- boolean operators: and, or, not
-- comparisons: == ~= < > <= >=
-- concatenation operator: ..  (coerces numeric arg to a string if other is)


while <exp> do
  <block>
end

repeat
  <block>
until <exp>

if <exp> then
  <block>
{elseif <exp> then
  <block>}
[else
  <block>]
end
